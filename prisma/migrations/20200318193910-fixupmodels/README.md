# Migration `20200318193910-fixupmodels`

This migration has been generated by Kim Fransman at 3/18/2020, 7:39:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Event" DROP CONSTRAINT IF EXiSTS "Event_course_fkey",
DROP CONSTRAINT IF EXiSTS "Event_season_fkey";

ALTER TABLE "public"."FinalInfo" DROP CONSTRAINT IF EXiSTS "FinalInfo_winner_fkey";

ALTER TABLE "public"."Hole" DROP CONSTRAINT IF EXiSTS "Hole_course_fkey";

ALTER TABLE "public"."Score" DROP CONSTRAINT IF EXiSTS "Score_event_fkey",
DROP CONSTRAINT IF EXiSTS "Score_player_fkey";

ALTER TABLE "public"."ScoringSession" DROP CONSTRAINT IF EXiSTS "ScoringSession_event_fkey";

ALTER TABLE "public"."Event" ADD FOREIGN KEY ("course")REFERENCES "public"."Course"("id") ON DELETE RESTRICT  ON UPDATE CASCADE

ALTER TABLE "public"."Event" ADD FOREIGN KEY ("season")REFERENCES "public"."Season"("id") ON DELETE RESTRICT  ON UPDATE CASCADE

ALTER TABLE "public"."FinalInfo" ADD FOREIGN KEY ("winner")REFERENCES "public"."Player"("id") ON DELETE RESTRICT  ON UPDATE CASCADE

ALTER TABLE "public"."Hole" ADD FOREIGN KEY ("course")REFERENCES "public"."Course"("id") ON DELETE RESTRICT  ON UPDATE CASCADE

ALTER TABLE "public"."Score" ADD FOREIGN KEY ("event")REFERENCES "public"."Event"("id") ON DELETE RESTRICT  ON UPDATE CASCADE

ALTER TABLE "public"."Score" ADD FOREIGN KEY ("player")REFERENCES "public"."Player"("id") ON DELETE RESTRICT  ON UPDATE CASCADE

ALTER TABLE "public"."ScoringSession" ADD FOREIGN KEY ("event")REFERENCES "public"."Event"("id") ON DELETE RESTRICT  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200315135436-initial-migration..20200318193910-fixupmodels
--- datamodel.dml
+++ datamodel.dml
@@ -1,14 +1,14 @@
-generator client {
-  provider      = "prisma-client-js"
-  binaryTargets = ["native"]
-}
-
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
+generator prisma_client {
+  provider      = "prisma-client-js"
+  binaryTargets = ["darwin"]
+}
+
 model Course {
   id     Int     @id @default(autoincrement())
   club   String
   name   String
@@ -40,9 +40,8 @@
   id        Int     @id @default(autoincrement())
   firstName String
   lastName  String
   photo     String?
-  scores    Score[]
 }
 model Score {
   id          Int    @id @default(autoincrement())
@@ -77,13 +76,12 @@
 model ScoringSession {
   id          Int      @id @default(autoincrement())
   event       Event
   currentHole Int      @default(value: 1)
-  player      Player[]
+  players     Player[]
   teams       Team[]
 }
-
 enum EventStatus {
   STARTED FINISHED
 }
```
